{
  "name": "astral-letters",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "dependencies": {
    "next": "15.2.5",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "lint": "npm run lint --workspace web",
    "db:generate": "npm run prisma:generate --workspace web",
    "db:migrate": "npm run prisma:migrate --workspace web",
    "db:seed": "npm run prisma:seed --workspace web"
  }
}
